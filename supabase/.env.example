# Copy → supabase/.env.local (file này KHÔNG commit — đã gitignore)
# Dùng cho: npx supabase link / functions deploy / db push

# Lấy token: https://supabase.com/dashboard/account/tokens
SUPABASE_ACCESS_TOKEN=sbp_your_token_here

# Project Settings → General → Reference ID (phải trùng URL trong apps/*/.env)
SUPABASE_PROJECT_REF=dwkmaaslsdqxebigoayj
